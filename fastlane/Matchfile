# Matchfile — fastlane match configuration for code signing.
# M3: Code signing setup via fastlane match.
#
# Match stores certificates and provisioning profiles in a git repo
# (encrypted). Create a private repo for this, or use fastlane's
# storage backend.
git_url("TODO_FILL_IN_MATCH_GIT_URL")
storage_mode("git")
type("appstore")  # can also be "development", "adhoc"
app_identifier("cloud.dkroeker.GaussianSplattingViewer")
