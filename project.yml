# XcodeGen project spec for the Gaussian Splatting Viewer iOS PoC (M1).
# Generate the .xcodeproj on a Mac (or in CI) with:  xcodegen generate
name: GaussianSplattingViewer
options:
  bundleIdPrefix: cloud.dkroeker
  deploymentTarget:
    iOS: "18.0"
  createIntermediateGroups: true
  groupSortPosition: top

packages:
  MetalSplatter:
    url: https://github.com/scier/MetalSplatter.git
    from: "1.0.0"

settings:
  base:
    SWIFT_VERSION: "6.0"
    ENABLE_BITCODE: NO

targets:
  GaussianSplattingViewer:
    type: application
    platform: iOS
    sources:
      - path: App
      - path: Scene
      - path: Model
      - path: Util
    dependencies:
      - package: MetalSplatter
        product: MetalSplatter
      - package: MetalSplatter
        product: SplatIO
    info:
      path: Info.plist
      properties:
        CFBundleDisplayName: Splat Viewer
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        UILaunchScreen:
          UIColorName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: cloud.dkroeker.GaussianSplattingViewer
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
